[project]
name = "pda-lab"
version = "0.1.0"
description = "Experiment-first lab for optimizing Progressive De-Arbitrarization handoff artifacts"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dspy>=3.1.3",
]

[tool.uv]
package = false
